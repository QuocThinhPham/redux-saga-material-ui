const styles = () => ({
   card: {
      marginBottom: 10,
   },
   cardHeader: {
      paddingBottom: 10,
      borderBottom: '1px solid rgba(0, 0, 0, .2)',
   },
	cardActions: {
		display: "flex",
		alignItems: "center",
		justifyContent: "flex-end"
	}
});

export default styles;
